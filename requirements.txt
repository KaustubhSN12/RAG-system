
fastapi
uvicorn
pydantic
streamlit
sentence-transformers
faiss-cpu
numpy
python-dotenv
openai
requests
pytest

# Embedding model (runs locally, free, no API key needed)
sentence-transformers==3.0.1

# Vector similarity search library
faiss-cpu==1.8.0

# Numerical operations
numpy==1.26.4

# Used to call an LLM for the "generation" step (optional - see query.py)
openai==1.35.10

# Loads OPENAI_API_KEY from a .env file so you never paste secrets into code
python-dotenv==1.0.1

